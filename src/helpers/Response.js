class Response {
  constructor(resposeData, message, responseType) {
    this.message = message;
    this.resposeData = resposeData;
    this.responseType = responseType;
  }
}

export default Response;
